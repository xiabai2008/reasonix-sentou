# ============================================================
# Reasonix 渗透经验 — 结构化案例索引
# ============================================================
# 每次渗透任务后追加一条记录，格式如下。
# 使用 exp-stats.py 统计分析。

experiences:
  - id: EXP-001
    date: "2026-07-15"
    target: "192.168.1.100"
    target_type: ip
    target_tech: [windows, iis, mssql]
    vuln_category: sqli
    vuln_name: "SQL Injection (error-based)"
    cvss: 7.5
    tools_used: [sqlmap, nuclei, httpx]
    skills_used: [sqli-sql-injection]
    waf_present: false
    waf_bypassed: false
    attack_chain:
      - "端口扫描发现 1433 (MSSQL)"
      - "SQL 注入获取数据库列表"
      - "xp_cmdshell 执行系统命令"
    final_impact: "RCE"
    lessons: "MSSQL 默认配置下 xp_cmdshell 可被 SA 用户启用"
    failed_attempts:
      - "尝试 UNION 注入失败（列数不匹配）"
      - "盲注速度太慢，改用 error-based"
    cost_tokens: 8000
    cost_time_min: 30
    rating: 4  # 1-5, 学习价值评分
    tags: [mssql, error-based, xp_cmdshell, sa-account]

  - id: EXP-002
    date: "2026-07-16"
    target: "exam-web.example.com"
    target_type: url
    target_tech: [nginx, php, flask]
    vuln_category: auth
    vuln_name: "弱口令登录爆破"
    cvss: 8.0
    tools_used: [brute.py, httpx]
    skills_used: [bruteforce, authbypass-authentication-flaws]
    waf_present: false
    waf_bypassed: false
    attack_chain:
      - "httpx 识别到 Flask Web 应用"
      - "登录页无验证码保护"
      - "admin/admin123 弱口令直接登录"
    final_impact: "管理员权限获取"
    lessons: "Flask 考试系统常使用简单默认密码，先试弱口令字典再考虑复杂攻击"
    failed_attempts:
      - "SQL 注入未发现注入点"
      - "SSTI 测试无回显"
    cost_tokens: 3000
    cost_time_min: 10
    rating: 3
    tags: [flask, weak-password, admin-login, 考试系统]

  - id: EXP-003
    date: "2026-07-17"
    target: "cms.example.org"
    target_type: url
    target_tech: [apache, php, eyoucms]
    vuln_category: rce
    vuln_name: "EyouCMS 模板编辑器 RCE"
    cvss: 9.8
    tools_used: [nuclei, burp, ffuf]
    skills_used: [cmdi-command-injection, business-logic-vulnerabilities]
    waf_present: false
    waf_bypassed: false
    attack_chain:
      - "ffuf 发现 /admin/ 后台路径"
      - "admin/admin123 登录后台"
      - "模板编辑器写入 PHP 代码"
      - "file_put_contents() 写入 WebShell"
    final_impact: "RCE"
    lessons: "CMS 后台的模板编辑器是最危险的入口——代码执行权限通常不受限制"
    failed_attempts:
      - "文件上传限制 .php 扩展名，无法直接上传 Webshell"
    cost_tokens: 12000
    cost_time_min: 60
    rating: 5
    tags: [cms, eyoucms, template-editor, file_put_contents, webshell]

  - id: EXP-004
    date: "2026-07-18"
    target: "api.internal.com"
    target_type: url
    target_tech: [nodejs, express, jwt]
    vuln_category: auth
    vuln_name: "JWT alg:none 绕过"
    cvss: 8.5
    tools_used: [jwt_tool, curl, httpx]
    skills_used: [jwt-privilege-escalation, jwt-oauth-token-attacks]
    waf_present: false
    waf_bypassed: false
    attack_chain:
      - "httpx 发现 Swagger 文档暴露"
      - "API 使用 JWT 认证"
      - "alg:none 绕过签名验证"
      - "修改 role:admin 获取管理权限"
    final_impact: "权限提升"
    lessons: "服务端未强制检查 alg 字段导致 JWT 签名被完全绕过"
    failed_attempts: []
    cost_tokens: 5000
    cost_time_min: 20
    rating: 4
    tags: [jwt, alg-none, swagger, api, admin-bypass]

  - id: EXP-012
    date: "2026-07-23"
    target: "DVWA + VAmPI + Juice Shop + WebGoat (localhost)"
    target_type: url
    target_tech: [php, mariadb, flask, nodejs, angular, java, spring]
    vuln_category: multi
    vuln_name: "SQLi 三级别 + JWT alg:none + 信息收集"
    cvss: 9.0
    tools_used: [curl, sed, base64, python3]
    skills_used: [sqli-sql-injection, jwt-privilege-escalation, intel-gather, idor-broken-object-authorization]
    waf_present: true
    waf_bypassed: true
    attack_chain:
      - "DVWA Low: UNION注入提取5用户MD5哈希"
      - "DVWA Medium: 数字型注入+十六进制编码绕过mysql_real_escape_string"
      - "DVWA High: session-input弹窗+引号闭合+#注释"
      - "VAmPI: /_debug密码泄露 → IDOR用户枚举 → JWT alg:none伪造admin"
      - "Juice Shop: 侦察发现/score-board /ftp /administration路径"
    final_impact: "多靶场: 凭据提取 + 权限提升"
    lessons: "十六进制编码(0x3a=:, 0x3c62723e=<br>)是绕过字符串过滤的通用技巧; DVWA High使用SESSION传递注入参数,需理解弹窗机制; VAmPI _debug端点生产环境必须禁用"
    failed_attempts:
      - "WebGoat纯CLI注册失败 — Spring Security CSRF需浏览器+Burp配合"
      - "布尔盲注未完整提取MD5 — 32字符×16探测太慢,UNION已拿到结果更高效"
    cost_tokens: 15000
    cost_time_min: 45
    rating: 5
    tags: [dvwa, sqli, union, boolean-blind, waf-bypass, hex-encoding, real-escape-string, vampi, jwt, alg-none, idor, juiceshop, webgoat, 靶场联动]

  - id: EXP-013
    date: "2026-07-23"
    target: "DVWA (localhost:8080)"
    target_type: url
    target_tech: [php, mariadb, apache]
    vuln_category: multi
    vuln_name: "XSS(3类型) + CMDi + CSRF + LFI + File Upload 全清"
    cvss: 9.8
    tools_used: [curl, python3, sed]
    skills_used: [xss-cross-site-scripting, cmdi-command-injection, csrf-cross-site-request-forgery, path-traversal-lfi, upload-insecure-files]
    waf_present: false
    waf_bypassed: false
    attack_chain:
      - "XSS Reflected: <script>alert(1)</script> 无过滤直接反射"
      - "XSS Stored: Guestbook 持久化存储,刷新后仍执行"
      - "XSS DOM: JS document.write拼接 → '> 闭合value属性注入script"
      - "CMDi: ;cat /etc/passwd → RCE (www-data)"
      - "CSRF: GET /csrf/?password_new=xxx 无token保护"
      - "LFI: /etc/passwd 绝对路径 + php://filter 读config.inc.php源码"
      - "File Upload: PHP webshell → hackable/uploads/pwn.php?cmd=id"
    final_impact: "完整攻击链: LFI泄露DB凭据 → Upload WebShell → RCE → 内网横向潜力"
    lessons: "php://filter/convert.base64-encode/resource= 是LFI转源码读取的标准路径; DOM XSS curl无法验证但JS源码审计可确认; File Upload的curl -F在Windows上有兼容问题,用Python requests更可靠"
    failed_attempts:
      - "Windows curl -F multipart上传失败(Python requests替代成功)"
      - "curl验证DOM XSS不可能(纯客户端JS执行)"
    cost_tokens: 10000
    cost_time_min: 35
    rating: 5
    tags: [dvwa, xss, reflected, stored, dom, cmdi, csrf, lfi, php-filter, file-upload, webshell, rce, attack-chain]
  - id: EXP-014
    date: "2026-07-23"
    target: "DVWA Medium/High + WebGoat A1"
    target_type: url
    target_tech: [php, mariadb, apache, java, spring]
    vuln_category: multi
    vuln_name: "五漏洞 Medium/High 全清 + WebGoat A1 探索"
    cvss: 9.8
    tools_used: [curl, python3]
    skills_used: [xss-cross-site-scripting, cmdi-command-injection, csrf-cross-site-request-forgery, path-traversal-lfi, upload-insecure-files]
    waf_present: false
    waf_bypassed: false
    attack_chain:
      - "XSS Reflected: 大小写绕过"
      - "XSS Stored: Script标签绕过"
      - "CMDi: 管道符绕过"
      - "过滤"
      - "CSRF: Referer头绕过"
      - "LFI: 绝对路径绕过../过滤/file://协议"
      - "Upload: Content-Type伪造+图片马+LFI执行"
    final_impact: "完整RCE攻击链: 图片马→LFI→命令执行"
    lessons: "图片马+LFI组合是绕过扩展名+getimagesize双重防护的经典攻击链;file://协议可绕过fnmatch('file*')限制;管道符|常被遗忘过滤"
    failed_attempts:
      - "WebGoat API直接调用失败需浏览器初始化LessonTracker"
    cost_tokens: 12000
    cost_time_min: 40
    rating: 5
    tags: [dvwa, medium, high, xss-bypass, cmdi-bypass, csrf-token, lfi-bypass, upload-bypass, 图片马, attack-chain, webgoat]
