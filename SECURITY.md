# 安全策略（Security Policy）

## 支持的版本

| 版本 | 支持状态 |
|:-----|:---------|
| master | ✅ 活跃维护 |

## 上报安全漏洞

**请勿在公开 issue 中提交安全漏洞。**

本项目不维护在线服务，主要风险集中在：
- 脚本中的命令注入 / 路径穿越
- 配置模板中的越权默认值
- 字典 / 模板引入的恶意载荷

如需上报，请：
1. 通过 GitHub 私有漏洞上报（Security → Report a vulnerability），或
2. 联系维护者（见仓库主页），附最小复现与影响描述。

我们会尽快响应，并在修复后公开披露（如需）。

## 本项目自身的安全边界

作为渗透测试工具集，本项目内置了以下护栏，贡献者不得削弱：

- 默认 `permissions.mode = "ask"`，高危操作需人工确认。
- 扫描前 `config/scope.yaml` 授权白名单校验。
- 反幻觉证据闭环：结论必须带 `EVID` 编号且逐字符校验，否则标 `[UNVERIFIED]`。
- 禁止 `.gov` / `.mil` 目标；禁止访问 `169.254.169.254`。
- 工具本体与字典不入库，避免供应链投毒风险。