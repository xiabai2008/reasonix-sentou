# ============================================================
# Reasonix 渗透作战工作台 — Python 依赖
# ============================================================
# 安装: pip install -r tools/requirements.txt
#
# [核心] 所有脚本必需
requests>=2.31.0           # HTTP 请求 (brute.py, orchestrator 等)

# [爆破] 登录爆破工具 (config/brute.py)
ddddocr>=1.5.0             # 验证码 OCR 识别 (可选: 缺失时爆破功能仍可用但无验证码识别)
Pillow>=10.0.0             # 图片预处理 (可选: 缺失时验证码图片不做增强处理)

# [配置] 授权范围校验 (scripts/check-scope.py)
PyYAML>=6.0                # YAML 解析 (可选: 内置简易解析器作为 fallback)

# [Dev] 开发与测试 (可选)
# pytest>=8.0.0            # 单元测试
# ruff>=0.5.0              # 代码格式/检查
